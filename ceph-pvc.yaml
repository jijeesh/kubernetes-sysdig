---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: sysdig-scap-storage-pvc2
    spec:
      storageClassName: "rook-ceph-block"
      accessModes:
        - ReadWriteMany
      resources:
        requests:
          storage: 6Gi
