---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: sysdig-scap-storage-pvc
    spec:
      storageClassName: "rook-ceph-block"
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 5Gi
