---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: sysdig-scap-storage-pvc
    spec:
      storageClassName: "nfs"
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 2000Mi
