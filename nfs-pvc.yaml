---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: sysdig-scap-storage-pvc2
    spec:
      storageClassName: "nfs"
      accessModes:
        - ReadWriteMany
      resources:
        requests:
          storage: 2Gi
