---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: sysdig-scap-storage-pvc
    spec:
      storageClassName: "nfs"
      accessModes:
        - ReadWriteMany
      resources:
        requests:
          storage: 2Gi
